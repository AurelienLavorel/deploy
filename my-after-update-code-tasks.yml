---
- name: Updates paths
  shell: find "{{ deploy_to }}/current" -name '*.{{ item }}' -exec bash -c 'cp -f $0 ${0/.{{ item }}/}' {} \;
  with_items:
    - "{{ deploy_environment }}"
    - "{{ lavoweb_remove_sufix }}"